# What is this

This is a playground for cross-domain iframe messaging.

# setup up
use your /etc/hosts to create two names for the same IP in which the docker exposes the port.
Make one of those with the name "b.dev" (will be the name of the iframe's host)

# usage
    docker-compose up
